<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.apps.bacon.mydiabetes">

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyDiabetes">
        <activity android:name=".LanguageSettings"/>
        <activity android:name=".ThemeSettings"/>
        <activity
            android:name=".CameraActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ScannerCameraActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ProductBarcodeActivity" />
        <activity android:name=".FoodPlateActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ChangeProductNameActivity" />
        <activity android:name=".AddTagActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".SaveProductActivity" />
        <activity
            android:name=".HomeActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>